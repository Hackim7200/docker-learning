#docker-compose.yml
version: '3.8' # file formatting version

services:
  myapp:    # container 1 myapp
    build: .
    ports:
      - "5002:5002"
    depends_on:
      - mydb

  mydb:     # container 2 database
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: my-secret-pw